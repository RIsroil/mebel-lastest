<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="042-rename-bonus-to-extra" author="dev">
        <comment>Rename BONUS to EXTRA in earnings and BONUS_PAID to EXTRA_PAID in financial_logs</comment>

        <!-- Update existing earnings from BONUS to EXTRA (VARCHAR column) -->
        <sql>UPDATE earnings SET earn_type = 'EXTRA' WHERE earn_type = 'BONUS';</sql>

        <!-- Update existing financial logs from BONUS_PAID to EXTRA_PAID (VARCHAR column) -->
        <sql>UPDATE financial_logs SET log_type = 'EXTRA_PAID' WHERE log_type = 'BONUS_PAID';</sql>
    </changeSet>

</databaseChangeLog>
