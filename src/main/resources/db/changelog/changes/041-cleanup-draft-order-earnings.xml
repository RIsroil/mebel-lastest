<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="041-cleanup-draft-order-earnings" author="claude">
        <comment>
            DRAFT holatdagi buyurtmalar uchun noto'g'ri yaratilgan earning va financial_log yozuvlarini o'chirish.
            Maosh faqat IN_PROGRESS buyurtmalar uchun hisoblanishi kerak.
        </comment>

        <!-- DRAFT buyurtmalar uchun yaratilgan financial_logs ni o'chirish -->
        <sql>
            DELETE FROM financial_logs
            WHERE reference_id IN (
                SELECT id FROM furniture_orders WHERE status = 'DRAFT'
            )
            AND log_type = 'WAGE_CALCULATED';
        </sql>

        <!-- DRAFT buyurtmalar uchun yaratilgan earnings ni soft-delete qilish -->
        <sql>
            UPDATE earnings
            SET deleted_at = NOW()
            WHERE furniture_order_id IN (
                SELECT id FROM furniture_orders WHERE status = 'DRAFT'
            )
            AND deleted_at IS NULL;
        </sql>
    </changeSet>

</databaseChangeLog>
