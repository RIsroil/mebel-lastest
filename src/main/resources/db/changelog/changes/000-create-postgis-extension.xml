<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="1772612580914" author="Isroil" dbms="postgresql">
        <sql>
            CREATE EXTENSION IF NOT EXISTS postgis;
        </sql>
    </changeSet>

    <changeSet id="000b-create-postgis-topology" author="Isroil" dbms="postgresql">
        <sql>
            -- Create topology extension if available on the server
            CREATE EXTENSION IF NOT EXISTS postgis_topology;
        </sql>
    </changeSet>

    <changeSet id="000c-create-postgis-vector" author="Isroil" dbms="postgresql">
        <!-- Only try to create postgis_vector if the server provides the extension files -->
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">
                SELECT COUNT(*) FROM pg_available_extensions WHERE name = 'postgis_vector'
            </sqlCheck>
        </preConditions>
        <sql>
            CREATE EXTENSION IF NOT EXISTS postgis_vector;
        </sql>
    </changeSet>

    <changeSet id="000d-create-postgis-version-view" author="Isroil" dbms="postgresql">
        <!-- Only create the helper view if postgis extension is installed -->
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">
                SELECT COUNT(*) FROM pg_extension WHERE extname = 'postgis'
            </sqlCheck>
        </preConditions>
        <sql>
            -- A simple view to show PostGIS version without using procedural languages
            CREATE OR REPLACE VIEW test_postgis_version AS
            SELECT 'PostGIS ready: ' || COALESCE((SELECT extversion FROM pg_extension WHERE extname = 'postgis'), 'unknown') AS status;
        </sql>
    </changeSet>

</databaseChangeLog>
